package com.noxa.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JwtConfig {
    // Empty — properties are read from application.properties
}